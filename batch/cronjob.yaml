apiVersion: batch/v1
kind: CronJob
metadata:
  name: voc-report
spec:
  schedule: "*/3 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: voc-agent
              image: atfleeacr0707.azurecr.io/voc-agent:v1
